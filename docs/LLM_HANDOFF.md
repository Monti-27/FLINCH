# FLINCH handoff

## Reference-led bento pair, 2026-09-11

Only the first two miniatures are redesigned. Custody now uses four equal Solana-marked chips settling into a recessed tray beneath fixed WSOL typography. Rollup uses a layered Control panel with Queue/Batch/Return progression, a 16px return and stationary token custody. First-two bottom fades are removed; the other three scene files and shared scene stylesheet match `artifacts/runs/bento-depth-before-wUletG/source.tar`. All five loop in 4.05 seconds with a 300/30 spring, replacing the rejected 6.2-second pacing. Preserve the concurrent footer-control removal; this task did not edit footer code. No replay or disclaimer UI was restored.

The supplied three images plus public BentoGrids/Magic UI references informed depth, focal hierarchy and readable small interfaces. No new video was attached. See BENTO_DESIGN for sources, the storyboard and truth boundaries. Control movement is not token movement or a confirmed sale. All protocol, wallet and financial handlers remain untouched.

Production `artifacts/runs/bento-1789075905574/result.json` verifies two complete loops at seven widths from 320–1920px, per-frame descendant/ancestor clipping, return/reset clearance, all five loops, live reduced motion, hidden/offscreen stops, no JavaScript and no external requests. Minimum Control gap is 13.97px and edge clearance 20px. Text checks meet 4.5:1. Production landing-only regression: `landing-1789075709353`; no wallet or arena transaction was attempted. Both type checks and 403 current web tests pass; source and selected-artifact scans cover 317 modules and 658 files. Isolated production build `/tmp/flinch-bento-depth.kUasKd/apps/web` passes with the existing optional bigint-binding warning. Bento source matches the snapshot. Later unrelated changes are not covered by that build.

Live landing: `http://127.0.0.1:3400/#onchain`. Port 3400 was not stopped or reset. The temporary 3412 verification server is not the user preview. Do not restore the backup over concurrent work. No dependency, funding, deployment, publication or Git initialization. Full protocol/devnet/ten-round, cross-browser/physical-device and visual-acceptance gates remain open.

## Finished hero hierarchy and neutral F, 2026-09-11

The user's follow-up rejects the previous detached captions and split text/actions. Hero now has one centered reading path: immediate two-line headline, short factual copy, charcoal 48px primary action, quiet How to play anchor and build disclosure. The four-column dot scene follows the copy; HTML Hold/Sell labels align exactly to each figure and stay 12px on mobile. A separate compact dot pattern avoids tiny solid-looking silhouettes. No hero heading blur or floating slogans remain. This supersedes the asymmetric hero below; preserve concurrent bento/footer refinements.

The F's exact geometry is unchanged. Shared `brand.css` fixes its color separately from link/heading animation: shadow-grey-950 on light landing/recovery/footer surfaces and alabaster-grey-100 on dark arena/loading/error/footer surfaces. The generator uses the same silver for light SVG downloads, browser and Apple icons. No palette primitive or ecosystem-logo change. See brand.md and LANDING_PAGE.

Final evidence: `landing-1789075585969` and `brand-colors-1789075754341`. Responsive alignment, figure/label pairing, keyboard, motion/no-JavaScript, routes and 20 logo geometry/color/contrast cases pass. All 403 web tests/38 files, both type checks, build and selected scans pass; earlier concurrent bento assertion failures have resolved. Preview `http://127.0.0.1:3411`, compiled copy `/tmp/flinch-hero-craft-Ae0YOI`, exec session 26712. The old 3410 hero preview is retired. Backup `artifacts/runs/hero-craft-before-KjB0fF/source.tar`. No sandbox reset, wallet transaction, funding, deployment, Git or protocol work. Visual acceptance and protocol/devnet/physical-device release gates remain open.

## Contained light footer, 2026-09-11

The follow-up removes the redundant closing CTA, footer slogan and footer motion button. Landing Footer now renders inside the inner page rails with `theme="light"`; the arena retains the dark default. Reference hand masks have stronger character alpha without new poses. A protected center clearance prevents character/wordmark overlap. `lib/shader/hand-flow.ts` renders continuously bending inward color bands; the shared shader lifecycle accepts the renderer and cadence while retaining its existing Waves defaults. Motion stops for reduced preference/offscreen/hidden state and static artwork remains without WebGL/JavaScript. See FOOTER for the storyboard, original-site research and source-resolution/licensing limits. The removed shared pause-button browser expectation was updated; the ambient store remains intact.

Final proof: `footer-browser-1789075714116`, `faq-1789075715436`, `landing-1789075716657`. Both footer themes pass 320–1920px alignment, no-overlap, targets, real framebuffer/masked-image variation, context recovery, native rules and static fallback checks. All 403 web tests, both type checks, isolated production build, 314-module comment/size scan and 652-file selected secret/artifact scan pass. The current footer, masks, shader and landing composition compare exactly with the compiled preview at `http://127.0.0.1:3120/#footer`, snapshot `/tmp/flinch-footer-contained-Rb6i2n`, exec session 42121. The app preview opening was queued by Codex. Before-state: `artifacts/runs/footer-contained-NmQeEE/before.tar.gz`.

Concurrent hero, bento, brand, chart and financial work was preserved. Do not restore the snapshot over it. No dependency, Git initialization, publication, deployment, funding, pool or wallet transaction was performed. Existing funded port 3400 and other previews were not stopped. Full protocol/devnet/ten-round gates, real-device testing, redistribution rights and user visual acceptance remain open.

## Dot-matrix hero, 2026-09-11

The user's new ASCII-style screenshot supersedes the mountain hero only. `hero-standoff.tsx` and `hero.module.css` now render an original indigo four-player illustration, with one figure stepping away from three holders, over a neutral dotted field. Headline/caption share the page rails; copy and native actions use responsive editorial columns. All four players remain visible on small screens. The half-second departure is finite and disabled by reduced motion; no JavaScript is needed for the illustration or content. No live data is implied. The old photo component/visible attribution are removed, with the photo asset, licensing record and source backup retained.

Final production evidence: `artifacts/runs/landing-1789074403674/result.json`. Responsive alignment, 44px hero actions, keyboard/focus, no-JavaScript content, live reduced motion, finite art motion, anchors, FAQ/rules, arena navigation, legacy redirects and 404 recovery pass with no page errors or landing external/feed requests. Current web suite: 386 tests/37 files; both type checks, build, 299-module source scan and 633-file selected scan pass. See LANDING_PAGE and TESTING for scope and provenance. This current result supersedes earlier concurrent snapshot failures involving the removed landscape attribution.

Preview: `http://127.0.0.1:3410`, isolated compiled copy `/tmp/flinch-hero-preview-ISkwCt`, exec session 49110; recheck before reuse. Pre-change archive: `artifacts/runs/hero-before-9u51w9/source.tar`. Do not restore it over other work. Bento, FAQ, footer, palette and arena are not redesigned. Port 3400's observed PID 28129 was not stopped or restarted by this task. No dependencies, wallet transactions, funding, deployment, Git initialization or publication. Full-chain/devnet/ten-round, physical-device/cross-browser and visual-acceptance gates remain open.

## Looping bento refinement, 2026-09-11

The latest user request supersedes the one-shot attempt: all five bento miniatures must loop, but only the first two designs change. Custody now keeps the WSOL label, official mark and four seat positions stationary; fills/faces animate. Rollup uses a horizontal intent lane, a gathering Control batch and a stationary Solana custody rail. The third through fifth scene components are unchanged. Replay buttons, the bottom disclaimer and the bento pause button remain removed. Do not restore centre-out stake tickets or the vertical rollup timeline.

The shared spring is slower and overdamped, with four reading holds and staggered details. Scene phases/cycles remain in scoped Zustand; Framer Motion interpolates. The existing footer switch now uses `ambientMotionPaused` to pause both decorative systems. Footer markup, artwork and CSS stay unchanged; only its preference binding is renamed. System reduced motion, offscreen and hidden-document stops remain. No financial number, wallet or game-timer behavior changed.

Production `bento-1789074047321` verifies two cycles per card, 320–1920px, real intermediate motion, no movement of WSOL/card/base geometry, existing keyboard pause/resume, no-JS/reduced motion, official marks and no external requests. Frozen landing/404 `landing-1789074138127` passes; newer concurrent hero tests fail against that older hero snapshot, not the bento. Final web snapshot: 386 tests/37 files and both type checks pass. The isolated production build passes with all type checks enabled; see TESTING for build setup failures and scope limits.

Live bento: `http://127.0.0.1:3400/#onchain`. The funded base genesis is unchanged; this task did not reset it or sign anything. The temporary 3411 validation listener was stopped after testing. Frozen source: `/tmp/flinch-bento-workspace.PTtrJI/apps/web`; backup: `artifacts/runs/bento-refine-before-JHNcCt/complete-source.tar`. Never restore over concurrent work. Read BENTO_DESIGN for the current storyboard. User visual acceptance, cross-browser/physical-device and protocol/devnet release gates remain open.

## Stake control panel, 2026-09-11

The supplied `skiper22.mp4` now informs a compact lobby control: official Solana header, centered rolling draft, fixed-height native exact input, 44px plus/minus, a native slider, Max and sliding presets/mode selection. Functional Zustand updates preserve every rapid click. Native typing stays static; only explicit adjustments roll. Bigint validation and the 0.001–0.01 SOL program range are unchanged. Create stays attached to the form rather than moving with chart height. Read STAKE_CONTROL for ownership and the motion/input contract.

Final production browser proof: `stake-browser-1789073911466`, including both directions, interruption, exact nine-decimal entry, keyboard/drag/touch, live reduced motion and 320–1920px targets. Document-relative Create bottoms are below 879px at a 900px viewport. `stake-control-5gNCIA/layout-result.json` passes component/layout/ecosystem/chart helpers with zero page errors. All 78 focused tests and both current TypeScript checks pass; frozen production snapshot `artifacts/runs/stake-production-3xDjgL` builds and has 372 passing web tests. Current whole-suite landing/bento failures occurred during concurrent edits; do not present the frozen count as a current whole-workspace pass. Full UI smoke stopped at the unrelated no-JavaScript arena footer expectation.

Read-only preview: `http://127.0.0.1:3502/play`, exec session 83667, built from that frozen source; verify before reusing. Source/video study and pre-change lobby archive: `artifacts/runs/stake-control-5gNCIA`. No new dependency, protocol change, onchain round, Git initialization, publication, deployment or faucet request. The funded sandbox on 3400 was not reset. Other tasks changed shared number, chart, footer, landing and startup files; never restore the snapshot over those edits. Devnet/full-cycle/ten-round and physical-device/visual-acceptance gates remain unverified.

## White reference accordion, 2026-09-11

The user rejected the initial dark FAQ and a visible expansion snap. `faq-accordion.tsx`, `faq-motion.ts`, `faq-flow.ts` and `faq.module.css` now preserve the white landing and use measured Framer springs, connected/separated card groups and a 1.1-second decorative blue/amber downward sweep. Native disclosures and original financial answers remain intact. The snap was reproduced by frame sampling: an unchanged MotionValue did not emit a style update, so native `open` expanded before animation. Explicitly locking the current height before opening fixes it. Do not remove that lock. See FAQ_MOTION and TESTING.

Rapid reversal, changing answers/wrapping, keyboard, native opening, no JavaScript, live reduced motion and repeated color-flow playback have browser regressions. The hands footer sources match the pre-FAQ archive exactly. An isolated production preview avoids interference with concurrent chart/bento work and the funded port-3400 sandbox. No protocol, wallet, network funding, deployment, Git initialization or publication is part of this task. Visual acceptance and full devnet release gates remain open.

## Black chart refinement, 2026-09-11

The user rejected the blue chart gradient and requested professional native chart views, smooth real updates and clean time controls. The chart now has a near-black plot, neutral chrome, teal/red candles, 1m/2m/5m and a native Candles/Hollow candles/OHLC bars/Line/Mountain selector. Line is unfilled; Mountain is neutral. Volume defaults off. Real ticks are frame-batched without interpolation, and historical viewport anchors survive retention/reconnect resets. Fullscreen Escape/focus and the feed-retry overlay were fixed through actual browser failures. Only chart semantic tokens change; palette primitives, financial handlers and concurrent number/entry/footer/landing edits remain preserved. See CHART for ownership and evidence.

Production proof: `chart-dark-1789072685363`, 350 exact upstream closed-candle matches, six observed Coinbase ticks, all styles/periods, 320–1920px, desktop alignment, mobile order, native keyboard controls, fullscreen, reduced motion, outage/retry and offline recovery. No runtime errors. Coinbase is genuine offchain SOL/USD reference, explicitly not Solana onchain data, the MagicBlock oracle or executable Raydium output. No fake fallback values. Focused suite 99 tests; full snapshot 376 web tests, type checks/build/scans pass. Broader UI smoke stops at a stale unrelated ecosystem selector. The taller concurrent entry panel places Create below the 900px fold at some widths; chart alignment passes, but above-fold entry is not claimed.

Source backup: `artifacts/runs/chart-dark-before-YvPzCn/source.tar`. Production snapshot: `/tmp/flinch-chart-verified.9JVF2T`. Do not restore backups over concurrent work. The funded sandbox remains on port 3400 with its original PID/genesis. No wallet transaction, deployment, faucet, pool, Git initialization or publication occurred; full protocol/devnet/ten-round gates were not rerun.

Final current-source chart browser run also passes on the preserved port-3400 sandbox: `chart-dark-1789072833383`, including the concurrent stable price-row styling. The later whole-repo type check is blocked by ongoing landing bento icon-size/type and test-state changes, not chart diagnostics. Do not present the earlier green snapshot as current whole-repo status. The temporary chart-only production listener on 3410 was stopped; other previews/sandbox were untouched.

## Shared rolling numbers, 2026-09-11

The user's number request is implemented in `components/ui/count-down-numbers.tsx`, with exact normalization/direction in `number-value.ts` and scoped CSS. Live prices, signed window percentages, player assets and current entitlement consume it. NumberFlow 0.6.2 supports exact decimal strings; the app overrides its float-based default trend with bigint comparison. Explicit accessibility role, no fake initial zero, one-second green/red feedback, reserved arrows, identity resets, timer cleanup and live reduced motion are covered. Signed minima, quote bounds, receipts, withdrawals and editable input stay static. The separate radial timer and all footer work are preserved.

The app already supports TypeScript/Tailwind. `components.json` plus the source alias establish its existing `src/components/ui` as the shadcn target; semantic utilities map to current tokens. Added the supplied Badge API using CVA 0.7.1; reused Lucide. Package installs disabled lifecycle scripts. Read NUMBER_ANIMATION before extending the precision or motion policy.

Final checks: 32 focused tests; 376-test whole-web snapshot; both type checks and production builds; 295 source/625 selected-file scans. Browser proof is `number-flow-1789072418274` (isolated exact/rapid/motion/accessibility tests) and `number-live-1789072510301` (real feed, production navigation/layout/controls). A wider UI run stopped at a concurrently outdated chart-selector assertion, not a number assertion. Full-chain/devnet release gates were not rerun. Frozen build source and archive: `artifacts/runs/number-production-6rDp3V`; original source backup: `number-flow-before-7tyKYA`. Preserve the funded 3400 sandbox and its genesis. Concurrent source changed throughout; see TESTING for exact snapshot distinctions and incomplete runs.

## Radial game timer, 2026-09-11

Follow-up: the seconds now sit inside a larger 160px ring (116–140px compact), with no left divider. Each bar has a fixed inner anchor and its own length spring; active bars extend, inactive bars retract, and a signed edge ripple lets neighbours move in opposite directions. This supersedes the side-by-side layout and single crest below. The second video study retains 144 close-spaced crops. It is a frame-informed reconstruction, not a verified exact copy of the source animation. See TIMER for evidence and limits.

Updated verification: 16 focused tests, full-countdown/geometry/bar-motion `timer-1789072832507`, and actual arena `timer-arena-1789072986424` pass. A 376-web-test snapshot passed, then concurrent bento changes blocked full type checking/build; root type check and selected scans pass. An isolated build additionally hit a Webpack WasmHash error. No unrelated landing code was edited. Use the existing port-3400 game for this revision; its genesis is preserved. The old port-3500 production preview and an unsuccessful isolated preview attempt are stopped, superseding the preview notes below. See TIMER for the failed snapshot's hoisted-dependency limitation. No onchain/release gate was newly completed.

The requested video-led game timer is implemented in `features/match/round-timer`, `timer-dial` and `timer-state`. A compact 72-ray circle replaces the old rectangular track; every bar is lit at 90 seconds, then drains with a bounded moving crest and blue/green/amber colors. Framer Motion rolls seconds upward beside the ring with fixed geometry. `Match` supplies millisecond precision from the same observed base Clock; no transaction/admission/financial behavior changes. Funding remains static, stale estimates remain explicit, live reduced motion is supported, and only actual terminal state/cancellation displays END. See [timer design and evidence](TIMER.md).

Evidence: 15 focused tests, 317-test web snapshot, both type checks/build, source and selected artifact scans pass. `timer-1789071116054` verifies every countdown second, real-time interpolation and reduced motion; production `timer-arena-1789071537639` verifies 320–1920px, adjacent controls, rules focus and reload with no page errors. Broader `ui-smoke-1789071254273` stops at the chart fullscreen Escape/focus assertion, not the timer. No fresh onchain/devnet cycle was run. Existing release gates remain. Reference frames and pre-change source are retained in `artifacts/runs/timer-reference-20260911`.

Read-only production preview: port 3500, exec session 71772; verify before reuse. Existing sandbox port 3400 and its genesis were preserved. Concurrent footer/chart/shared-number work was not edited; later changes are not covered by this timer snapshot. No dependency, deployment, funding or Git initialization was performed by this task.

## Footer handoff, 2026-09-11

The user's reference-video footer is implemented. All 915 frames were decoded/reviewed; actual frame-600 ASCII hand cutouts are the mask assets, not new hand drawings. `footer-hands.tsx` owns inward spring entrances and visibility/pause behavior, with continuous inward masked colors and a persistent Flinch × MagicBlock center. FOOTER.md documents provenance, full storyboard and licensing/resolution limits. This replaces the previously protected Waves footer only; preserve unrelated concurrent chart, landing, notification and timer work.

Final production footer evidence: `artifacts/runs/footer-browser-1789071269513/result.json`, six widths, motion/visibility/reduced-motion/no-JavaScript, assets, native keyboard controls and zero page errors. The 76 focused footer/brand/contrast tests, production build, both type checks and selected scans pass. Broader UI run `ui-smoke-1789070999569` passed its footer chrome checks then failed the unrelated chart fullscreen/Escape assertion; no chart fix was attempted. No chain/devnet round was rerun. See TESTING for full details.

A separate read-only production snapshot runs at `http://127.0.0.1:3100` from `/tmp/flinch-footer-build-oN0GJq`, exec session 88045. Its three footer source files compare exactly to the workspace; unrelated surfaces are a snapshot of concurrent work. Verify availability before reuse. The user's port-3400 sandbox/genesis and existing port-3000 preview were not stopped. Backup and complete reference sheets: `artifacts/runs/footer-hands-7Iu1E6`. Do not restore the archive over concurrent work.

## Current task

2026-09-11 single-bento follow-up: the user dropped the old four-rule card band and requested redesigning only the second ER tile. Removed `GameStory`, `RuleIllustration` and `story.module.css`. Landing links now point to `#onchain`; `#how-it-works` is a zero-size legacy alias. The rollup uses a dedicated stylesheet and one integrated white workspace: Control/Sell intent ticket, fanned batch sheets, sliding Intent/Batch/Return underline and a stationary Solana custody row. Other four scene files and footer source/CSS match `artifacts/runs/rollup-before-WKJ6Bx/source.tar`.

Verified: both type checks, build, 363 web tests; later source/selected-artifact scans cover 285 modules and 611 files including concurrent work. Focused browser `bento-1789071869102` verifies 320–1920px, all motion controls, real intermediate ticket motion with zero card/custody shift, no-JS, official marks and no external requests. Landing/404 regression `landing-1789071968744` verifies one grid and current/legacy anchors with arena navigation skipped. No wallet or chain action occurred; the funded sandbox remains on 3400. Reference and storyboard: BENTO_DESIGN. Earlier instructions to preserve the old rule cards are superseded by this explicit request. Visual acceptance, physical-device/cross-browser and all public-devnet/release gates remain open.

### Previous bento implementation

2026-09-11 protocol bento: the user supplied `0dBZFHNDQQxaXYQ9-optimized.mp4` and requested close, smooth product-interface animation. Replaced only the staggered protocol steps at `#onchain` with a three-top/two-bottom grid. `features/landing/bento` separates five miniatures, scoped Zustand phases/pause, Framer Motion lifecycle and CSS. Official ecosystem marks, fixed outer cards, native replay/pause, reduced-motion/no-JS stills, visibility stops and accurate intent/custody/withdrawal copy remain. Read BENTO_DESIGN for reference frames, storyboard and architecture. Hero, rules, FAQ, game and footer were not edited by this task.

Final focused browser: `artifacts/runs/bento-1789070885750/result.json`; existing landing/404 regression: `landing-1789070527739` with arena navigation deliberately skipped. Both type checks, build and a 269-web-test snapshot pass. A later source/artifact scan passes 269 modules and 588 selected files, including additional concurrent work; it does not extend earlier build evidence to later unrelated edits. Local 4x-CPU probe: p95 9.2ms, maximum 41.2ms; not a universal smoothness guarantee. Layouts 320–1920px, real intermediate motion with stable geometry, keyboard/mouse replay, pause, reduced motion, synthetic hidden-document events, no-JS and same-origin-only requests are verified. No onchain/release gate is newly completed.

Landing: `http://127.0.0.1:3400/#onchain`; existing game: `/play`. Do not reset the funded sandbox or run fixed-port stack suites over it. Source backup: `artifacts/runs/bento-before-nR3Mpv/source.tar`; video study: `bento-reference-20260911`. Never restore backups over concurrent landing/chart/footer work. User visual acceptance, cross-browser/device checks and all existing public-devnet authorization/release gates remain open.

### Concurrent chart task

2026-09-11 chart task: the user requested KLineCharts and aligned trading controls. Implemented pinned 10.0.3 with a client-only renderer, native candles/line, observed volume, UTC axes, crosshair/pan/reset/fullscreen and both-edge desktop alignment. Read CHART.md for modules, partial-volume policy and evidence. Chart-scoped 46 tests, final full-suite 269 tests, both type checks, build and scans pass. Intermediate footer expectation failures were resolved by the parallel footer task, not edited here. Production-browser checks cover 320–1920px and chart interactions. The new standalone chart helper was not run. The port-3400 sandbox and all financial handlers remain untouched; release/devnet gates remain unverified. Never restore the source backup over concurrent landing/footer work.

### Concurrent landing task

2026-09-11 reference-led landing and 404: implemented the two user-supplied video directions in `features/landing` and `features/not-found`. The landing now uses scoped pale paper, fine rails, compact centered typography, word-by-word blur reveals, inset rules, stepped process cards and native FAQ. The former parallax and pinned story/store are retired. The 404 has original dot-matrix house/flower art, bounded click/touch/keyboard planting, clear and real recovery links. No new footer, protocol behavior or dependency was introduced by this task. Native scrolling, no-JavaScript reading/recovery, live reduced motion, wallet/market isolation and room-invite compatibility remain. See LANDING_PAGE for provenance and exact motion policy.

Final source browser proof: `landing-1789069807449` passes both surfaces and arena navigation; production UI `ui-smoke-1789069808544` passes on the same build. A successful workspace snapshot has 257 web tests, both type checks/build, 252 source modules and 565 selected scanned files. Source/video-study backup is `artifacts/runs/landing-reference-SXEXSo`; final motion frames and timing are retained there. The live preview is loopback port 3000, exec session 18467; verify before reuse. The existing funded sandbox on 3400 was not stopped or reset. No new onchain/devnet proof is claimed.

Concurrent work matters: notification and chart files changed in other tasks during this pass. A chart-only server import failure resolved in that parallel work before the final passing build. Footer component/CSS then changed after the successful production capture; this task did not edit or revert them. Its shared Footer invocation follows that current component. Nine other protected shader/brand/palette/token files still match the archive. Do not describe the late footer revision as validated by the earlier UI run or restore older source over it. User visual acceptance and existing release gates remain open.

### Parallel notification-copy follow-up

2026-09-11 notification-copy follow-up: replaced generic confirmation text with action-specific Sonner titles/descriptions. Wallet cancellation now shows a blue "Request cancelled" toast instead of being dismissed and echoed as raw text. `notifications.ts` owns outcome copy, `action-errors.ts` handles nested rejection codes and safe error messages, and `notify-action.ts` prevents duplicate notifications across action and component catches. Errors no longer live in the transaction/claim UI stores or form paragraphs. Persistent pending guidance, proof, field focus, drafts and connection availability remain. A pending journal always overrides cancellation-looking text. No signing, protocol, footer or sandbox reset change.

The rejection-only browser fixture uses an existing local public address and no secret key. It reaches real unsigned create previews, rejects every wallet request, verifies one toast and no inline error at 375/768/1280px, checks genuine-error/validation variants and confirms zero submissions, no journal entries and unchanged balance. Final evidence: `artifacts/runs/notification-ux-1789068184645/result.json`; the earlier run caught screenshots mid-animation and was strengthened with settled-geometry assertions. Full tests/build/scans pass; see TESTING. Unrelated concurrent landing files were not edited. No public devnet or full game cycle is claimed; existing release gates remain open.

### Earlier Sonner placement follow-up

2026-09-11 notification follow-up: the user explicitly requested the official Sonner component, bottom-right placement and per-situation colors. `NotificationToaster` centralizes one dark rich-color host with safe-area offsets. Dedicated notification CSS no longer washes out Sonner's native status backgrounds/text. `confirmationTone` selects green for base confirmation, blue for ER intent acceptance, amber for uncertainty and red for failure; loading is neutral and wallet cancellation stays quiet. This overrides older top-positioned toast guidance. Footer, wallet signing fix and sandbox are unchanged. Live isolated visual checks cover all five variants, contrast, desktop/tablet/mobile placement, long errors and keyboard dismissal. The temporary preview route was removed before the production build. See TESTING for final checks.

### Previous wallet-signing task

2026-09-11 wallet-signing fix: the user's Phantom create-room response triggered the client's strict message check. The connected wallet had already received one explicitly authorized synthetic local SOL after its absent-account preview failure. Preserve the running sandbox and its genesis; restarting loses that grant. The exact failed Phantom signed response was not captured. Official Phantom docs confirm automatic fee insertion on eligible unsigned transactions, and offline fee-enhancing-wallet plus legacy-reconstruction tests reproduced the exception.

`transaction-budget.ts` now makes missing compute settings explicit before preview/signing (zero priority fee; 200,000 units per application instruction, capped at 1,400,000) while preserving existing budgets and rejecting duplicates/invalid limits. `transactions.ts` uses the legacy compiler for legacy messages and checks packet size before wallet prompting. `transaction-message.ts` retains exact-byte equality after preview/signing and reports only changed categories; no signed bytes/keys are logged or stored. This shared policy also applies to session SELL and new keeper preparations. No protocol, UI, footer, dependency or deployment change.

Passing checks: 40 client, 28 keeper, 23 runtime, 18 harness, 188 web, both type checks/build, domain 35 tests/format/Clippy, generated client and source/artifact checks. Twelve regressions added. `wallet-signing-ITUQWH/runtime` retains actual LiteSVM create/deposit/recover/claim signatures and snapshots. Live unsigned create for the user succeeds on the existing base, and a zero-value local-ER simulation accepts the budget. The served port-3400 arena contains the patch. Full Phantom signing/submission remains unverified until the user reloads and retries; do not claim the extension is proven. The active sandbox must not be stopped to run fixed-port stack/browser suites. Full devnet/release gates remain incomplete. See TESTING and LOCAL_PLAY.

### Previous landing task

2026-09-09 landing page: implemented the user's premium/storytelling landing request using the supplied Fora video as compositional inspiration and a licensed Sergey Pesterev Unsplash landscape. `/` is now a server-rendered introduction; `/play` retains the game. `features/landing` owns modular copy, landscape motion, scroll-stage illustration and FAQ/protocol sections. Native scrolling, immediate text/CTAs, scoped Zustand, reduced motion and no-JavaScript content are preserved. The landing mounts no wallet/RPC/market provider. Wallet-adapter CSS moved to `/play` after browser tests caught its global Google-font request. Full source/license/motion details: LANDING_PAGE and UI_DESIGN.

New invites use `/play?room=…`; parsing accepts same-origin legacy `/` links, and the landing redirects only one valid room parameter, dropping unrelated fields. Local tooling advertises `/play`. The footer's visual design/shader and eleven protected files are unchanged; its component accepts an optional real arena link for the landing. No dependencies, onchain code or financial logic changed. Backup: `artifacts/runs/landing-before-Jaz4E1/source.tar`.

Final: 187 web tests, both type checks, production build, 219 source constraints and 521-file scan pass. `landing-1788958898598` verifies 320–1920px, scroll forward/reverse, keyboard/FAQ/rules, no-JavaScript, reduced motion, route compatibility, no external landing HTTP traffic and sampled hero contrast (minimum 4.83). Local 4x-CPU frame probe: p95 9.3ms, maximum 16.7ms; not a device-performance guarantee. Existing production UI `ui-smoke-1788957856926`, critical two-swap/four-claim `mb-stack-r7OkEF`, and four-browser three-swap/four-claim `mb-stack-TNOimT` pass. See TESTING for retained failures and limitations. No fresh Rust/SBF run.

Read-only production preview: `http://127.0.0.1:3000`, arena `/play`, exec session 38144, listener PID 86390; verify before reuse. Test services stopped. User visual acceptance and physical-device/cross-browser checks remain open. The broader release goal remains incomplete, with the previous ordinary-wallet, persistent-world, replay/soak/dependency and unauthorized public-devnet gates unchanged.

### Previous withdrawal slice

2026-09-09 withdrawal continuation: the latest icon-only turn verified existing work but did not advance the larger goal. This turn implemented a focused claim UX and preparation-safety slice. `withdrawal-state.ts` derives base-only entitlements and composition; `withdrawal-ticket.tsx` plus its CSS module own the amount/action/details; `claim-action-store.ts` scopes in-flight/error feedback by client/room/wallet and rejects duplicate preparation. Match replaces the redundant sell panel for settled/closed/recovery states. Known pending base operations disable claims but not separately journaled recovery. No local success flag removes balances. Preserve the approved footer and all existing brand primitives.

Final evidence: 183 web tests, both type checks, production build, 208 source modules and 504 scanned files. Critical browser `mb-stack-XE2dEI` completes two swaps/four claims/reload/revocation and adds responsive withdrawal hit tests, wallet rejection without signing, unchanged claimable balance and a real base USDC claim through an injected ER read outage. Four-browser `mb-stack-HS3z8c` completes four UI deposits/three swaps/four claims and explicitly checks final-holder WSOL typography and base-derived closed state. Production UI `ui-smoke-1788953851220` passes. Read TESTING for earlier passing iterations and limits. Desktop/mobile and final-holder screenshots were reviewed. No fresh Rust/SBF gate was needed for this application-only slice; do not imply it was rerun.

Read-only production preview is refreshed on `http://127.0.0.1:3000`, exec session 62665; verify before reuse. Test 3300/3400 servers, base/ER validators and keepers stopped. Backup: `artifacts/runs/withdrawal-before-u8VgfQ/source.tar`. Eleven protected footer/shader/brand/token files compare exactly. No dependency changes or copied Pro code. Live Collect UI wallet-gallery and Skiper Aave preview inspection informed the hierarchy, not protocol behavior.

The goal remains active. Next prioritize the persistent-local-world and ordinary-wallet workflow plus the remaining reliability/release gates, instead of repeatedly rechecking completed icons or shifting the same layout. Old-generation replay, multi-room contention, soak, dependency advisories and historical unexplained domain rejection remain. Public devnet deployment/funding/pool setup and ten sequential rounds are not authorized or complete. User visual acceptance and device/cross-browser performance remain open.

### Previous delayed-commit slice

2026-09-09 delayed-commit continuation: final `mb-stack-Gf6l3y` proves a real frozen commitment delayed through expiry, an old signed fill rejected with a still-valid blockhash, paired execute/expire submissions, no extra penalty and subsequent fresh sale/four claims. Final `mb-stack-yaBBOI` holds an actual commitment across hard recovery: base recovery succeeds while Control stays delegated, prior-sale USDC is claimed before release, then the real late return cannot reopen the terminal Ledger. Three remaining players claim their exact penalty-enriched WSOL. 158/1,039 targeted committer HTTP attempts were blocked respectively. These are transport attempts, not economic effects. Earlier passing `37i2tj`/`jQZyT7` predate added evidence/receipt assertions.

New test modules: `base-service.ts`, `commit-outage.ts`, `rejected-transaction.ts`, `delayed-handoff.ts`, plus commit-outage harness tests. `bootstrap.ts` accepts an optional local base transport; default configuration is preserved. The separate base wrapper supports SIGINT, now an optional `stopChild` argument; default remains SIGTERM. Both exits are observed and cleanup attempts all services. No application/program/UI/dependency changes. Late fill attempts in these runs reject at base preflight, not as landed failed transactions. See TESTING for exact evidence and limits.

Final checks: 18 harness, 29 client, 28 keeper, 169 web, both type checks, 203 source modules and 498 selected scanned files. Default-bootstrap critical browser `mb-stack-zERd3A` passes two swaps/four claims/reload/revocation/read failures/quote expiry. Production UI `ui-smoke-1788901964566` passes; desktop arena, mobile active and desktop claim screenshots reviewed. No Rust/SBF/build rerun for this harness-only slice. All test services stopped; port 3000 remains the read-only preview. Backup: `artifacts/runs/late-handoff-before-SmAgwu/source.tar`.

Goal stays active. Next: remaining ordinary-wallet/persistent-world workflow, old-generation replay/multi-room/soak cases and dependency-advisory release work; keep improving actual UI components where a concrete user-facing gap is observed. Local delayed return and expiry/recovery now have proof, not exhaustive stale-generation or public-devnet proof. Public deployment/funding/pool setup, ten sequential devnet rounds and visual acceptance remain incomplete and require their existing gates. Preserve the footer/official assets.

### Previous independent-keeper and session slice

2026-09-09 fault-test continuation: independent-keeper and ER-visible session-revocation scenarios now pass. `tests/stack/competing-keepers.ts` runs two actual CLIs with separate keys/journals and pairs signed submissions through a test-only RPC gate. Final `mb-stack-gsoOx6` proves two overlapping batches, two swaps, two receipts, four exact claims, terminal reconciliation by both keepers and no CPI/token changes in losing transactions. Earlier `mb-stack-vYY9mk` passed before failure-metadata assertions. Gate/proxy helpers are separately tested; neither is production infrastructure.

`tests/stack/session-revocation.ts`, `mb-stack-gD0NsU`, proves a warm actual session intent, no-penalty expiry, successful session simulation before base revoke, ER-visible account removal, authorization rejection and a failed transaction without Control/custody changes. Direct wallet fallback then completes two actual swaps/four claims. Rejection was first observed 3ms after the harness observed base confirmation; this is not a hosted propagation bound. Revocation does not cancel previously accepted intents. Retained iterations `mb-stack-kTSBcC` and `mb-stack-C8DRpV` exposed test assumptions about missing send-error logs and failed signatures despite send errors; both are documented in TESTING.

Only tests, package scripts and documentation changed in this slice. No production/UI/dependency/program changes. Focused checks: 15 harness, 29 client, 28 keeper, 169 web, both type checks, 198 modules and 493 selected scanned files pass. Full Rust/SBF/build/browser was not rerun; previous evidence follows. Test processes stopped. Read-only port 3000 responded HTTP 200; verify before reuse. Backup from the start of this slice: `artifacts/runs/keeper-races-before-zIz8Ew/source.tar`.

Goal remains active. Next: deliberately late/stale commitments and fill/expiry/recovery races on the real stack, then remaining ordinary-wallet/persistent-world workflow and release gates. Local independent competition and revocation now have specific proof, but do not imply exhaustive faults, hosted devnet, ten sequential rounds or production readiness. Preserve footer/official assets and do not infer public deployment/funding/pool authority.

### Previous interactive-local slice

2026-09-09 interactive-local continuation: `tools/local` supplies explicit loopback startup on port 3400, bounded one-time synthetic grants, validated room watch, keeper pause/resume, service-exit supervision and staged cleanup. Read LOCAL_PLAY before running. No ordinary extension wallet is proven: the four-player test uses four isolated test Wallet Standard adapters signing real local transactions. Each sandbox launch creates a new genesis; durable sandbox resume is not implemented. Do not tell users that Phantom Devnet mode targets this local chain.

`LocalLifecycle` observes stack/web process exits, rejects continued readiness, drains every keeper, attempts remaining cleanup after errors and retains keys if keeper exit cannot be confirmed. Normal quit is idempotent. An actual web termination test passed in `mb-stack-YTL5nK`, with error exit 1 and all service ports freed. `status` reports individual keeper process health; unclean keeper exits retain the existing explicit lock-recovery requirement. No automatic process restart or lock stealing was added.

Room invitations are a compact toolbar utility with separate room-scoped Zustand state, clean same-origin links, manual clipboard-denial selection, keyboard/focus behavior and reduced-motion feedback. The lobby accepts pasted invites without signing. Live Collect UI Invite gallery inspection informed the component. Footer/shader/identity/palette are unchanged. Screenshot review exposed stale accepted-SELL toast copy after successful sale; it now reports the historical intent and points to the position for current sale status.

Four-browser evidence `mb-stack-LxT6yo` passes four UI deposits, three actual local Raydium swaps, four UI claims, exact vault/entitlement checks, keeper pause/queued-SELL/resume, duplicate funding/watch checks, invite keyboard/clipboard denial/motion/mobile tests and one pasted-link join. The earlier `mb-stack-omSnuW` passed before the notification correction; `mb-stack-XHWoPN` predates invite/supervision. The final screenshot was reviewed and no longer contradicts confirmed sales. No Control injection; synthetic mint/liquidity fixtures only.

Full verify-v2 passed during this slice: 35 domain debug/release, 18 legacy, one V2-ID, 29 client, 28 keeper, 10 harness, 23 runtime, 169 web, Clippy/SBF/IDL, both type checks/build and scans. Log `target/local-sharing-validation.log`; runtime `local-sharing-runtime-20260909`. After the copy-only correction, focused tests/type checks/build and a fresh four-browser round passed. Source: 192 handwritten modules; selected scan: 487 files. See TESTING for final UI/critical-browser follow-ups. Backup: `artifacts/runs/local-sharing-before-4kYP0B/source.tar`; earlier sandbox source backup remains `sandbox-before-OPDVsS/source.tar`.

Goal remains active. Next: real-extension local compatibility or an explicitly approved public devnet setup, persistent-world operating design, broader competing-worker/stale-commit/session-revocation faults and soak. Public deployment, funded pool, hosted keeper, ten devnet rounds, dependency release gates and user design acceptance remain incomplete. No deployment, public funding, faucet request, pool creation, Git initialization or publication occurred.

Final follow-ups passed: production UI `ui-smoke-1788899046257` and critical browser `mb-stack-pHfdZt` (two swaps/four claims/reload/revocation/read failures/quote expiry). Local test servers and validators are stopped. A fresh read-only production preview responds at `http://127.0.0.1:3000`, exec session 40995; verify before reuse. The enabled sandbox on 3400 is not left running. Final source and selected artifact checks remain 192 modules and 487 files.

2026-09-09 keeper continuation: implemented `apps/keeper/src/cli.ts` and small runtime modules for strict configuration, bounded owner-only files, dedicated signer loading, exclusive journal ownership, read-only inspection and explicit execution. The service reuses RoomWorker/runRooms without changing economic rules. `packages/client/src/routing/local.ts` supplies an explicit loopback-only SDK-backed resolver; test clients now use it too. Devnet routing is unchanged. No dependency, program or frontend presentation changes. Read KEEPER_OPERATIONS before running the service.

New evidence: final standalone `mb-stack-xKz2CH` starts a funded room, rejects a duplicate process, restarts from a pending execute with a different process ID, completes two swaps and four claims with a separate fee payer. `mb-stack-0CVyfZ` accepts session SELL, restarts with injected ER HTTP 503 responses, waits for the actual hard cutoff and performs base recovery plus four unchanged-WSOL claims; 32 failed ER requests, no swaps or game penalties. Neither injects Control. `mb-stack-KUZbed` is the earlier passing standalone iteration before the harness delegated round start to the service.

The critical browser test now runs the actual standalone keeper continuously, rather than manually ticking an in-process worker around screenshots. `mb-stack-ibMvXo` passes two swaps/four claims/reload/revocation/base-and-ER-read-recovery/quote-expiry with the independent service. The test adapter still signs player transactions; this is not extension-wallet proof. Final production UI `ui-smoke-1788894965224` passes; desktop claims and mobile active screenshots were reviewed. Footer, official icons and design remain unchanged.

Full verify-v2 passed with 29 client, 28 keeper, five harness, 23 runtime, 163 web, 35 domain debug/release, 18 legacy and one V2-ID tests, build and both type checks. Retained log: `target/keeper-service-validation.log`; runtime records: `artifacts/runs/keeper-service-runtime-20260909`. Later browser-harness integration and client-config field narrowing have separate final checks. Source now has 175 handwritten modules. Backup: `artifacts/runs/keeper-before-TupVIN/source.tar`.

Limits: a clean SIGTERM/Ctrl-C drains work and releases `keeper.lock`; an unclean crash intentionally leaves it and requires operator verification before exact lock removal. Do not delete room journals or auto-steal locks. Configuration and payer files must be mode 600, owner-only regular files, and payer files outside the repo; directory mode 700. Local test payer files were removed after processes exited. Hosted deployment, persistent ordinary-wallet local setup, real-extension testing, broader competing-worker/stale-commit/soak cases, dependency advisories and ten devnet rounds remain open. Goal stays incomplete. Next focus: usable persistent local operating workflow and ordinary-wallet configuration, then further faults; do not infer public execution authority. Production preview was refreshed at `http://127.0.0.1:3000` (read-only); verify availability before reuse.

2026-09-09: continuing the active end-to-end gameplay and improved-component-design goal. Official assets remain. The current frontend adds an original paired-token `SellTicket`, contained exact-string stake field, fixed-hit-area animated selectors, quieter filled seats and a compact chart that does not stretch with the ticket. Live Collect UI wallet/chart galleries, Jeet's Savings Vault and Skiper's public Aave swap preview informed the work; no Pro code, dependency or third-party component source was copied. Footer/shader/palette/identity are unchanged across ten protected files. Read the top sections of UI_DESIGN, FRONTEND and brand.md; older descriptions below are historical.

Core changes: client quotes are checked immediately before signing as well as after signing and before submission. `sell-request.ts` removes the redundant preliminary route lookup and hands the verified preparation endpoint to `OperationRunner`, which journals before broadcast. Two independent verified resolutions remain. `room-observation.ts` retains confirmed Ledger observations across transient failures but clears Control. Base/ER clocks are independent. `sell-state.ts` uses the client admission/full-quote rules. Focused tests prove wallet rejection, changed placement without submission, stale/expired quote rejection and retained observations.

Final evidence: full verify-v2 gate passed, with 163 web and 27 client tests. Final production UI `ui-smoke-1788892667389` and local browser game `mb-stack-ltcg1A` pass. The game adds base/ER outage recovery and explicit quote expiration/no-auto-signing/deliberate refresh to two swaps, four claims, reload and base revocation. Runtime snapshots: `core-ticket-runtime-20260909`. Source constraints: 158 modules. Retained iteration failures and exact scope are in TESTING. Screenshots were reviewed. Read-only production preview is `http://127.0.0.1:3000`; verify it remains live before reuse.

Goal remains active and incomplete. Next: ordinary-wallet local operating workflow and keeper lifecycle, additional fault/soak coverage, then separately authorized public devnet setup and ten sequential rounds. Do not claim public play or real-extension proof from the local adapter. Existing dependency advisories, historical intermittent domain rejection and release gates remain. No public deployment, funding, pool creation, Git initialization or publication was performed. Backup is `artifacts/runs/core-before-tI5HUV/source.tar`.

2026-09-08 follow-up: the user requested official online ecosystem icons. Self-hosted Solana, MagicBlock, Raydium and USDC assets now use the shared `components/brand/ecosystem-icon.tsx`. Sources and usage restrictions are in ECOSYSTEM_ASSETS. The footer toolbar replaces its three generic icons only; footer CSS, centered lockup and shader are unchanged. Protocol navigation, network credit, stake input, chart pair and withdrawal labels use the applicable marks. Financial logic is untouched. See TESTING for this follow-up's validation.

2026-09-08: the user requested a complete frontend redesign inspired heavily by Collect UI, with the existing footer unchanged. The actual starting point was the newer quiet instrument revision, not the Solpot revision below. Live dashboard and button galleries were visually studied; no third-party assets, registry snippets or new dependencies were added. The current direction is a charcoal game table, four horizontal desktop positions, pale indigo controls, underlined entry tabs, large exact-string amount entry and a bounded reference chart beside the action panel. Mobile keeps actions before the chart. Read the current sections of brand.md, UI_DESIGN and FRONTEND; older descriptions are historical.

`components/shell/arena-workspace.tsx` composes the shared overview, players, action panel, chart and optional history for lobby and match. Player presentation remains separate from ledger derivation. Compact player disclosure preserves keyboard/touch controls and inert hidden content. Empty seats only focus entry. Zero-entitlement claim rows are omitted while spectator recovery remains. Financial handlers, quotes, signing, settlement boundaries and exact integer amounts are unchanged. The approved split F and all 33 palette primitives remain exact. Eleven protected footer, shader, brand and palette files plus inherited footer tokens match the pre-change backup.

Final evidence: production UI `artifacts/runs/ui-smoke-1788887517052/result.json` and complete local game `artifacts/runs/mb-stack-usF205/result.json` both pass. The game completed two actual local Raydium swaps, four claims, a reload after SELL and base session revocation without Control injection. The quote action passes above-fold/hit tests at 375/768/1280px. Final desktop/mobile lobby, queued and claimable screenshots were reviewed. The 141-test web suite, both TypeScript checks, production build, 146-module source check and 431-file selected scan pass. TESTING records scope and remaining gates.

The refreshed read-only production preview is `http://127.0.0.1:3000`; verify availability in future sessions. Public gameplay is not deployed or enabled. Local validation uses synthetic liquidity and a test Wallet Standard adapter, not public devnet or extension-wallet proof. Full Rust/SBF checks were not rerun for this presentation-only pass. Ten devnet rounds, hosted services, real wallets, reliability and security release gates remain open. Await user visual feedback; tests do not establish perfect design or production readiness. No Git initialization, deployment, publish, faucet request or public pool creation occurred. The full previous frontend/tests/docs are recoverable from `artifacts/runs/design-before-collect-zAanCY/frontend.tar`.

### Previous Solpot component task

2026-09-08: the user supplied a Solpot screenshot and explicitly asked for better component construction, not another layout-only change. Live Solpot was visually inspected. The implemented direction is raised indigo actions, recessed amount input, three selector appearances, framed player positions, bold italic round headings and stronger tabular numerals. The approved split F, exact 33 palette primitives, existing notched navigation behavior and full-width Waves footer are preserved. No dependency or financial-protocol change was made. Current direction is at the top of brand.md, UI_DESIGN and FRONTEND; conflicting older styling below is historical.

New ownership: `features/match/player-seat.tsx` contains presentation while `roster.tsx` derives ledger data. `token-input.css`, `player-seat.css` and `wallet.css` separate surfaces by responsibility. `Button` defaults to `type="button"`; funding, claims, recovery, receipt retry and dialog confirmation use it alongside lobby/SELL. `SegmentedControl` exposes `inset`, `keys` and `tabs`. The wallet adapter discards custom classes, so style its actual trigger selector. The unused metallic-pill component and stylesheet were retired; the whole previous frontend/docs are recoverable from `artifacts/runs/design-before-solpot-40FxWW/frontend.tar`.

During verification, `mb-stack-YCYCEw` reached active play but failed mobile SELL visibility. The corrected position panel pairs balance and quote action; `mb-stack-E03x6p` and the subsequent `mb-stack-OYGLIK` both completed two local Raydium swaps, four claims, reload and base session revocation. Active SELL passes above-fold/hit tests at 375/768/1280px. These runs use synthetic local liquidity and a test Wallet Standard adapter, not public devnet or real extension-wallet proof. Narrow navigation overflow and nine-decimal input fit also received explicit regression checks. See TESTING for final production-browser evidence and validation counts.

Await user visual feedback. Do not claim perfect design, production readiness or completion of devnet/reliability/security release gates. No Git initialization, publish, deployment, public pool creation or faucet request occurred.

Final evidence: `ui-smoke-1788881212611` and `mb-stack-RNcwB0` pass after all button migrations. The web suite has 138 passing tests; both type checks, production build, 148-module source check and 433-file selected scan pass. Latest production preview is `http://127.0.0.1:3000`. The previous runs above are retained as iteration evidence; TESTING describes final scope and limitations.

### Previous approved logo integration

The user approved the split F logo on 2026-09-08 and requested site-wide integration. `components/brand/geometry.ts` is the canonical two-path source; `BrandMark`/`BrandLogo` supply shared, accessible inline SVG presentation. The header, existing shader-backed footer, loading/error states and 404 page use it. Below 481px the header shows the mark alone without crowding wallet/menu controls. The approved paths are unchanged; only framing, size and inherited color vary. Earlier logo proposals remain in artifacts and are not application dependencies.

`scripts/generate-brand-assets.mjs` creates dark/light public SVGs, `app/icon.svg`, a 16/32/48px `favicon.ico` and `apple-icon.png`. Production builds run its `--check` parity gate. No logo loader, runtime image service, dependency, financial change or shader redesign was added. Read the approved section in brand.md before further identity changes.

Verification: 129 web tests, both type checks, production build, 146-module no-comment/size and selected secret/artifact scan pass. Final production UI `ui-smoke-1788878513099` covers the brand at ten widths from 320 to 1920px, touch/focus/hit targets, identical geometry, asset/metadata responses, loading and 404 recovery, plus the full existing UI/feed/shader checks with zero page errors. Header/footer desktop, tablet and mobile screenshots and the fallback page were reviewed. The read-only preview was refreshed on `http://127.0.0.1:3000`. Backup: `artifacts/runs/logo-before-site-Y9qeFq/frontend.tar`. No fresh gameplay round or devnet proof is claimed; release limits in TESTING remain.

### Previous full-width shader footer

Latest request, 2026-09-08: full-width, borderless footer with the supplied 21st.dev Waves shader and centered “Flinch × Magicblocks” typography. Implemented in `footer.tsx`/`footer.css`, with self-hosted Space Grotesk and Manrope. The old column SVG is removed and backed up. Actual protocol links, native test-build disclosures, arena/rules actions and all financial semantics remain. All 33 selected palette primitives are unchanged; footer-only tokens follow the supplied four-color shader recipe and a tested contrast scrim.

`components/ui/adisyon-shader.tsx` is the small reusable React wrapper. `lib/shader` separates supplied GLSL, settings/pixel sizing, validated GPU resources and observer/motion/context lifecycle. The shader allocates when visible, caps rendering at 30fps/two million pixels, pauses offscreen/hidden and freezes for reduced motion. Manual pause is provider-scoped Zustand. Allocation/compile failure shows static CSS artwork; context restoration rebuilds the renderer. No new dependencies were installed. The wallet's Paper effect is still a CSS approximation, distinct from this actual footer shader.

Current checks: 120 web tests in 12 files, both TypeScript checks, production build, 141-module comment/size and selected secret/artifact scan pass. Final production UI run `ui-smoke-1788877359602` passes with zero page errors. It covers four full-width footer layouts, centered typography, real framebuffer variation, pause/reduced-motion/offscreen behavior, simulated visibility, WebGL loss/restoration, fallback and existing critical UI interactions. Desktop/tablet/mobile screenshots were reviewed. Device-wide frame-rate guarantees, Safari/Firefox and mobile GPUs remain unverified. No current gameplay round or public devnet success is claimed; `mb-stack-frcGIO` predates the chrome changes. Read TESTING for open release gates.

The preview was rebuilt on `http://127.0.0.1:3000`; verify it before sharing later. Previous footer source is recoverable from `artifacts/runs/footer-before-shader-yKkoEY/source.tar.gz`. Earlier backups remain untouched. No Git initialization, deployment, pool creation or faucet request occurred. Visual acceptance remains with the user.

### Previous terminal and component task

2026-09-08: the user superseded the Collect UI table with a Rugs.fun-like terminal, then explicitly rejected layout-only changes and supplied a Trove screenshot as component inspiration. Current UI has a persistent 256px desktop player list, compact round bar, chart then action dock, silver primary buttons, inset exact-string token entry, shared segmented selectors, filled seats and monospaced amounts. The SVG curved rail and repeated seat summaries are removed. Below 1100px players use explicit disclosure with inert collapsed content. The chart defaults to a monochrome area with optional candles. The latest source is modular and financial signing/quotes/settlement are unchanged. Do not copy Trove/V7 implementation, add chat/mining/clans, or restore the historical layouts below. Read brand.md and the current UI_DESIGN section first. Await user visual feedback, not test-derived claims of perfect design.

The new `SegmentedControl` and `TokenAmountInput` live in `components/ui`; `controls.css` owns reusable styling. Mode selection and drafts are provider-scoped Zustand. Eighty web tests currently pass, including four component checks and four additional contrast cases. Current verification evidence belongs in TESTING. Iteration runs `mb-stack-TNhL4A` and `mb-stack-AqjidL` reached active play but failed the 375px above-fold quote assertion; they are not successful complete rounds. The quote button now follows the balance before explanatory text. Backup: `artifacts/runs/design-before-terminal-5vxbmc/frontend.tar`. No Git initialization, dependency install, public deployment, pool funding or faucet request occurred.

Final current-code evidence: production UI `ui-smoke-1788812853829` and local game `mb-stack-frcGIO` pass. The latter completes two actual local swaps, four claims, reload and base session revocation without Control injection; the quote action passes above-fold and overlap checks at 375/768/1280px. Production smoke covers six widths, real feed/font loading, chart and entry controls, keyboard/touch/reduced-motion disclosure, offline recovery and explicit feed outage with zero page errors. Desktop/mobile lobby and funded/active/claimable screenshots were visually reviewed. All 80 web tests, both TypeScript checks, production build, 125-module source check and 307-file selected scan pass. The stale preview was replaced by a fresh read-only production server on loopback port 3000; verify it is still running before sharing later. The local stack uses synthetic liquidity and a test Wallet Standard adapter. Full Rust checks, ten devnet rounds, real extensions and release reliability/security gates remain outstanding; see TESTING. Design acceptance remains with the user.

### Previous task context

The user requested a substantial, carefully composed redesign inspired by Collect UI. The new game-table layout puts four real seat summaries and the round clock ahead of a compact reference chart, with entry/position controls in the aligned right column. Mobile orders round, actions, then chart. Preserve all 33 user-selected grey/indigo/alabaster primitives and self-hosted Space Grotesk/Manrope. Solid surfaces replace glass effects; financial controls do not lift or scale. The curved Framer player drawer remains accessible on every width. Summary buttons open details; empty drawer slots focus entry without submitting or reserving seats. Read [UI_DESIGN.md](UI_DESIGN.md), [FRONTEND.md](FRONTEND.md) and `brand.md`. Await user visual feedback; do not claim perfection. Zustand, Sonner, real charts and real-sales rules remain required. Follow [BUILD_PLAN.md](BUILD_PLAN.md), no comments and the 300-line handwritten TS/new Rust ceiling. Never edit or copy V7 implementation.

## Game-table slice, 2026-09-07

- Visually inspected Collect UI's Mac app and card galleries, including Taras and Rachit Thakur references. No third-party code/assets copied or new dependency installs.
- Source at the start had drifted from the documented prior pass: glass tokens, fixed-height scrolling, a noninteractive/hidden mobile rail and duplicate toast hosts. Seventeen existing unit checks failed. A source backup is retained at `artifacts/runs/design-before-table-WoUQGS/frontend.tar`.
- `standoff.tsx` and its CSS module own derived round presentation. `styles/lobby.css` owns the split workspace and forms. Buttons use typed native elements and coherent CSS states. Ledger, quote, session and transaction logic remains separate.
- Rail collapse uses inert plus accessibility hiding; focus restoration happens after the trigger becomes focusable. A hover guard prevents reopening under a stationary pointer after closing rules. The live reduced-motion preference is unchanged.
- One top-positioned Sonner host replaces duplicate hosts. The local browser scenario now hit-tests SELL at three widths, in addition to asserting it is above the fold. Read TESTING for final run evidence and outstanding production gates.
- Final verification completed 2026-09-08: production UI `ui-smoke-1788786444417` and local game `mb-stack-WXzKpo` pass. The latter completes two swaps, four claims, reload and base revocation without Control injection; active SELL passes overlap hit-tests at 375/768/1280px. Desktop/mobile screenshots were reviewed. This uses synthetic local liquidity and a test Wallet Standard adapter, not public devnet or a real extension.
- 71 web tests, both TypeScript checks, production build, 123-module source check and 304-file selected-pattern scan pass. The full Rust gate was not rerun. Public deployment, ten devnet rounds, real wallet-extension coverage and the historical domain rejection remain unresolved. The read-only production preview was left on loopback port 3000; verify it is still running before sharing it in a later session.

## Historical fullscreen slice

- Palette primitives and semantic roles remain separate. Indigo 600 with alabaster 50 passes body-text contrast; indigo 500 did not. Contrast suite covers 25 pairs plus all 33 exact colors.
- Fontsource variable Manrope and Space Grotesk are exact 5.3.0 pins, registry-checked and installed without lifecycle scripts. Local WOFF2 loading uses `next/font/local`; public OFL notices ship with the app. No SF files or runtime Google requests.
- Layout is isolated in shell/game/market CSS; the game component only composes the new lobby wrapper. Active SELL remains ahead of the chart. No financial or protocol changes.
- `layout-browser.ts` verifies 375/768/1024/1280/1440/1920 widths, full-viewport frame, compact bordered chart, desktop alignment, no overflow and actual loaded fonts. Existing UI smoke covers rail, keyboard, dialog, feed and outage cases. Exact settled rail shape/opacity checks avoid mid-animation screenshots.
- Web suite: 67 tests. Build, both TypeScript checks, 118-module no-comment/size check and 297-file selected-pattern scan pass. Current run evidence is in TESTING. Full Rust/SBF/Clippy, public devnet and real extension-wallet gates were not rerun or completed by this UI revision.
- Final production UI evidence: `ui-smoke-1788714136162`; final local cycle and funded-rail capture: `mb-stack-HGhJuG`. The funded screenshot uses a tall viewport before opening, avoiding full-page capture resizing during motion; active SELL checks restore 900px height. The loopback preview is read-only and remains running on port 3000. No public deployment, faucet request, pool creation or Git initialization occurred.

## Previous orange palette and rail slice

- User-palette rail: `palette.css` owns exact primitives; `tokens.css` owns semantic roles; `player-rail.css` owns the rail layout. Framer Motion 13.2.0 was checked for React 19 compatibility and installed with scripts disabled. Native SF/system type remains.
- `player-rail-state.ts` is the pure closed/hover/open reducer. `room-sidebar.tsx` owns accessible disclosure, `rail-shape.tsx` owns one spring shared by outline and clip, and `roster.tsx` owns real player presentation. Fine mouse hover is transient; touch/keyboard explicit opening is supported. Inert collapsed content, Escape, close, outside interaction and focus departure are handled. Main chart/action bounds do not move during the animation.
- Reduced-motion state is provider-scoped Zustand, with a live matchMedia subscription and safe reduced server default. The pinned Framer Motion hook snapshots at mount; do not replace this with that hook without rechecking upstream implementation. Motion storyboard lives in docs, not comments.
- Latest local rail-cycle evidence starts at `mb-stack-vgtc0u`: actual test wallet signing, two swaps/four claims, reload, revocation and a verified no-signature empty-slot action. Final-code test/build/browser evidence is recorded in TESTING. Web suite: 64 tests, including 22 semantic contrast pairs and all 33 exact palette primitives. Protocol tests were not all rerun for this UI-only slice.
- Final production UI evidence: `ui-smoke-1788711376368`; responsive-fix local game evidence: `mb-stack-ev9f0Y`. A repeat failure in `mb-stack-QiAtZh` revealed compact-width mouse hover intercepting SELL after resize. Compact layouts are now click/tap-only, breakpoint transitions dismiss, and the closed hit zone cannot extend into the canvas while its curve shrinks. Both regression paths pass. This failure is separate from the historical domain rejection. Final checks: 64 tests, build, both typechecks, 117-module comment/size check and 294-file selected scan. See TESTING for exact scope.

## Previous Collect UI slice

- Final Collect UI workspace evidence: `mb-stack-b4ezB3` (two local swaps, four claims, reload and base revocation) and `ui-smoke-1788708203784` (read-only production UI, actual Coinbase, keyboard/responsive/outage checks). Active SELL is above the fold at 375/768/1280. Production build, both TypeScript checks, 51 web tests, 114-module source check and 286-file selected-pattern scan pass. Final visual inspection includes queued, claimable and mobile states. Tests are local, not public devnet or real wallet-extension proof.
- Scoped Zustand factories separate UI preferences, room observations, wallet transaction display and market observations. The existing public operation journal and base/ER safety boundaries remain authoritative.
- Sonner complements inline status. ER confirmation means SELL intent accepted, never a completed sale. Tokens, penalties and quotes continue to use exact integer arithmetic.
- Lightweight Charts 5.2.1 renders actual Coinbase SOL-USD history/ticks. This is labeled offchain reference data, not MagicBlock oracle verification or a devnet executable quote. Reconnection, stale data, hidden/offline tabs, bounded history and disposal are handled. Live candles may be incomplete. No fabricated fallback prices.
- The latest palette is graphite with blue actions, lighter sidebar/control levels and native San Francisco/system typography. `brand.md` is the current token direction, not user approval. DynaPuff/DM Sans remain removed; no SF font redistribution or new dependency install.
- Desktop players live in a dedicated sidebar and become a compact grid on mobile. Lobby entry is attached below the chart; active funding/SELL/claim controls precede it. Advanced controls are expandable. Zero asset lines, empty live withdrawals and fully claimed action buttons are omitted. Confirmed exits replace holding instructions only from base-ledger state. Permissionless recovery stays visible, including for spectators. No protocol/accounting changes.
- Research read: Anthropic frontend-design, Vercel web-design-guidelines, WebKit system-font guidance, Radix color roles and NN/g visual hierarchy. Sources and historical Rugs study are retained in UI_DESIGN. No new globally installed skills.
- Latest web suite has 51 passing tests, including explicit queued-versus-confirmed exit, withdrawal visibility/recovery, composition and sixteen contrast cases. Source checks cover 114 handwritten TS/TSX modules. See TESTING for final build/browser/selected-scan evidence; this is not a full security/accessibility audit or a fresh full Rust gate.
- The initial Collect UI runs `mb-stack-Ee9mbA` and `mb-stack-ixMaNX` both completed two local swaps, four claims, one reload and base revocation. The second verifies the quote action is above the fold at 375/768/1280. Later state-copy refinements are tracked by the final evidence in TESTING. Tests use a Wallet Standard adapter and synthetic local liquidity; no Control injection, public deployment or real extension proof.
- Read-only UI runs `ui-smoke-1788707307660` and `ui-smoke-1788707661182` passed real Coinbase, responsive, keyboard, chart and outage checks. Final evidence is in TESTING. Old `mb-stack-jhwxBo` / `ui-smoke-1788641162057` belong to the rejected olive theme.
- Historical arcade-theme runs `mb-stack-WWuBho` and `mb-stack-2FkcV1` passed. `mb-stack-4SFwxL` failed connected-wallet mobile width before a header fix. Do not present these as current visual evidence; they are unrelated to the historical domain rejection below.
- `test:browser:ui` checks the read-only production UI, real public chart feed, controls, invalid-room feedback, dialog focus wrapping/return/Escape, reduced motion, responsive widths, offline recovery and an explicitly injected 503 history outage. Requires public Coinbase connectivity for its live-feed assertions. See TESTING for final evidence.
- No hosted keeper, public pool/deployment, ten-round devnet proof or clean dependency audit exists. Do not turn this frontend delivery into a production-readiness claim.

## Canonical direction

Four equal WSOL stakes, 90 seconds, two-second cohorts, 25 bps on current seller entitlements shared among remaining holders. Aggregate actual Raydium devnet WSOL → Circle USDC sale. No seller-to-seller penalties, no forced lottery, no penalty for failed fills, all-sell penalty waived. Natural final holder keeps WSOL; deadline survivors keep holdings.

Base Ledger and token custody never delegate. Control carries session-authorized intents on the public ER, freezes and undelegates per batch, is settled/expired on base, then synchronized and redelegated. No keeper-reported success. Hard cutoff at end plus 30 seconds permits base recovery without Control.

## Migration state

- Implemented `crates/flinch-domain` with validated snapshot/restore and 35 passing debug/release tests.
- Implemented staged `programs/flinch-v2`: base custody/refunds/recovery/claims, typed Raydium CPI, session-bound intents, delegation, freeze/return, execution and expiry. New Rust source/test files remain below 300 lines, without comments.
- 21 LiteSVM runtime tests, three harness tests, 18 legacy regressions and the V2 program-ID test pass. Full `scripts/verify-v2.sh` passes, including SBF/IDL, TypeScript and strict lint.
- Real local stack success: `artifacts/runs/mb-stack-40V4IJ/result.json` (two sell handoffs/four claims); `artifacts/runs/mb-stack-L7IqI6/result.json` (three handoffs, one expiry, two actual swaps/four claims). No injected Control in either stack test. The expiry run includes source hashes, signatures, commitment transactions and writable-account snapshots. Local mint/liquidity fixtures are synthetic, not Circle-issued balances.
- `programs/flinch` still contains the old eSPL/VRF program. Preserve until the replacement has runtime coverage; do not deploy it as real-sales FLINCH.
- Old docs/research remain historical. New generated artifacts use `flinch_v2`; do not mix their IDL with `flinch`.
- Shared client and keeper libraries now exist, with 16 client and 18 keeper tests. Generated contract drift, handwritten TypeScript comment/size checks and the new tests are part of `verify-v2.sh`.
- Client/keeper stack evidence: `mb-stack-UauAQ1` completed two swaps/four claims with six worker reconstructions; `mb-stack-Gwof2t` added a no-penalty expiry/new-nonce retry and eight reconstructions. Synthetic liquidity; no Control injection. See TESTING for latest reruns and caveats.
- Final-code reruns: `mb-stack-1i2ANq` normal and `mb-stack-YOOiaQ` expiry/retry both pass. Latest full extended check evidence: `local-v2-client-keeper-final-20260906`, with output in `target/v2-client-keeper-validation.log`.
- Executable quote adapter, safe quoted-intent preparation/submission and confirmed receipt decoding now exist. Quotes use shared Raydium layout validation and exact integers; 12 creator-fee/orientation combinations match actual local Raydium CPI output. Quotes expire after two seconds and are checked again after signing/before submission.
- `apps/web` now contains Next.js lobby, Wallet Standard adapter integration, memory-only session lifecycle, match, claims, public transaction reconciliation and confirmed receipt proof. Default is read-only. Browser tests use a test wallet to sign genuine local transactions; no test signer is bundled into the application.
- Initial quote/browser slice counts: 26 client tests, 23 runtime tests, five harness tests, six web unit tests; existing 35 domain/18 keeper/18 legacy/one V2-ID tests preserved. The redesign's newer web count is recorded above. Production web build passes. New frontend/quote source remains below 300 lines per module, without code comments.
- Final full gate: `local-v2-web-final-20260906`, log `target/v2-web-final-validation.log`; all source/runtime/web checks passed. Final browser run `mb-stack-HJMVOu` includes the corrected local placement observation, receipt display, two swaps, four claims, one reload and base revocation. Source checker covered 89 handwritten TS/TSX modules.
- No live deployment, hosted keeper service, funded devnet pool or ten-round devnet proof yet. Dependency audit reports advisories; read SECURITY before release.

## Next gate

Next harden the implemented quote/browser slice: real wallet-extension testing, browser recovery and reload at every handoff, stale-commit/competing-worker/revocation fault tests, operational keeper entrypoint, and dependency-advisory review. Continue investigating the unexplained local second-batch preflight rejection retained in `mb-stack-drBKZp`: `DomainRejected: seller has no remaining position`. Six additional normal runs and later quote/browser successes did not reproduce it; they do not explain it. The failure wrapper now retains writable-account snapshots and a same-signed-transaction simulation replay while preserving the original error. Replay may observe a later slot than the initial preflight.

Quote-backed stack proof: `mb-stack-fiFPGR` normal and `mb-stack-4WPPKb` expiry/retry. Browser proof: `mb-stack-H7sIfx` and `mb-stack-CppNlm`; the latter includes public wallet signatures, confirmed transactions and base session revocation after reload. Each completed two swaps and four claims. See TESTING for subsequent final-code runs.

Browser run `mb-stack-G1X2HG` exposed an insufficient-rent session signer top-up. Fixed by fetching rent-exempt minimum for a zero-data system account and adding a 100,000-lamport fee reserve. Generic browser transactions now simulate before signing and journaling. Session top-up is not a protocol-enforced spending allowance. Anchor namespace imports and an explicit browser Buffer import fixed Node/browser compatibility.

Browser run `mb-stack-uGdVfp` exposed an unrelated false placement rejection: local resolver context slot was incorrectly called delegationSlot. Local records now expose `observedSlot`; the client refreshes base at that floor and checks the ER snapshot against both base snapshots. Hosted router metadata still uses its actual delegationSlot. The deterministic regression and final browser run pass. Do not attribute the earlier domain rejection to this issue.

Browser journal scopes separate base actions, ER SELL and base recovery. Web Locks exclude simultaneous wallet use by cooperating tabs; public local storage survives reload. Unknown submissions remain pending; known quote rejection before any send is `not_sent`. Missing browser storage fails closed. This is not a distributed journal, full archival history, or a replacement for onchain replay guards. A connected keeper is still needed during gameplay.

The keeper persists public transaction identity before send, reconciles unknown status on restart, records supersession without inventing signature success, and never silently re-signs an uncertain fill. Disjoint expiry and hard recovery may supersede unknown work without trading. Recovery chooses base before contacting ER and skips unnecessary Control decoding. File journals require one active store/worker instance per directory; they are not distributed locks. Read CLIENT_KEEPER before extending.

Remaining milestone-3 adversarial checks include deliberately delayed stale commitments, duplicate-worker races, ER-visible session revocation and unavailable-ER recovery on the real stack. Runtime recovery and keeper outage mocks pass; do not equate those with an injected live service outage.

Do not make production code depend on tests/local fixtures. Hosted routing still needs `getDelegationStatus` FQDN/owner/validator checks; local stack routing only verifies its configured ER against the onchain delegation record and actual identity. Do not label that a hosted-router test.

The local helper uses legacy messages because early-genesis V0 transactions were accepted in preflight but dropped during validator sanitization on both Agave 2.3.0 and 3.1.10. Exact cause is not yet proven. The passing stack uses Agave 3.1.10 via child-only PATH and ER 0.14.10. No global tools were changed. Keep the size assertion; do not assume future transactions fit legacy messages.

The upstream `GetCommitmentSignature` helper caused a long observation delay in the second cycle. The harness now polls the same upstream log contract directly, records every returned base signature, confirms each base transaction, then verifies Control owner/phase/revision. This is an isolated compatibility adapter, not a new MagicBlock protocol. Permanent polling errors now fail immediately. Wait for the base Clock execution window; never widen the 15-second limit to pass a test.

Keep SDK/version updates local and pinned. RUSTUP_TOOLCHAIN selects stable 1.94.1 despite rust-toolchain.toml requesting 1.89.0; no host 1.89.0 is installed. SBF uses platform-tools v1.53. The old build CLI warns about standard syscalls despite successful SBF execution in both runtimes; keep this warning documented until the toolchain is reconciled. Anchor macro crates in Cargo.lock now match runtime 1.0.2. The only old implementation change is a crate-level allowance for an Anchor-generated Clippy warning. Raydium types/CPI come from the reviewed IDL, not an incompatible Raydium Rust crate.

The workspace has no Git repository. Git initialization, publication, deployment, faucet requests and pool funding require separate explicit authorization. No secrets in files or logs. No mainnet.
