[package]
name = "flinch-domain"
version = "0.1.0"
edition = "2021"
rust-version = "1.89.0"
description = "Deterministic accounting and settlement model for FLINCH"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
float_arithmetic = "deny"
